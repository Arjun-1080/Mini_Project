import React from 'react';

const SmallLoader = () => {
  return (
    <div>
        <div className="w-8 h-8 border-4 border-t-[var(--primary)] border-[var(--ternary)] rounded-full animate-spin"></div>
    </div>
  );
};

export default SmallLoader;